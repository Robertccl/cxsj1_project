package Manage;

import java.sql.SQLException;
import java.util.List;

import Model.Salesman;
import SalesmanOperate.*;
/*

public class ManageSalesman {
	
	public void managesalesman()
	{
		
		
		
		//querySalesman.query(name, number, sex, birthday, phone, employeddate, address, date);
		
		//removeSalesman.removeSalesman(id);
	}
	
	public void addsalesman() throws SQLException
	{
		Salesman temp=new Salesman();
		//Cin cin=new Cin();
		temp.set();
		querySalesman.query(name, number, sex, birthday, phone, employeddate, address, date);
		addSalesman.add(temp);
	}
	
	public void updatesalesman() throws Exception
	{
		Salesman temp=new Salesman();
		//Cin cin = new Cin();
		
		temp.set();
		querySalesman.query(name, number, sex, birthday, phone, employeddate, address, date);
		updateSalesman.updateSalesman(temp);
	}
	
	public Salesman Search()
	{
		
		querySalesman.query(name, number, sex, birthday, phone, employeddate, address, date);
		
	}
	
	public void Remove()
	{
		removeSalesman.removeSalesman(id);
	}
	
	
	protected Addsalesman addSalesman=new Addsalesman();
	protected UpdateSalesman updateSalesman=new UpdateSalesman();
	protected QuerySalesman querySalesman=new QuerySalesman();
	protected RemveSalesman removeSalesman=new RemveSalesman();
	
}

*/
