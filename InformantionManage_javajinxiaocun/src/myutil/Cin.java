package myutil;

import java.util.Scanner;

import Model.*;
/*
public class Cin {
	public Salesman cinSalesman()
	{
		Salesman temp=new Salesman();
		Scanner cin= new Scanner(System.in);
		temp.setName(cin.nextLine());
		temp.setNumber(cin.nextLine());
		temp.setGender(cin.nextLine());
		temp.setbirthDate(cin.nextLine());
		temp.setPhone(cin.nextLine());
		temp.setemployedDate(cin.nextLine());
		temp.setAddress(cin.nextLine());
		
		return temp;
		
	}
}
*/